import { RequireDirective } from './require.directive';

describe('RequireDirective', () => {
  it('should create an instance', () => {
    const directive = new RequireDirective();
    expect(directive).toBeTruthy();
  });
});
